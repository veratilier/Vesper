// Independent Vesper tables. Never import or initialize the original Desire database.
export const desireSchema = [
  "CREATE TABLE IF NOT EXISTS vesper_desire_state (\n  user_id TEXT PRIMARY KEY,\n  style TEXT NOT NULL CHECK (style IN ('quiet', 'playful', 'clingy')),\n  longing INTEGER NOT NULL CHECK (longing BETWEEN 0 AND 100),\n  tenderness INTEGER NOT NULL CHECK (tenderness BETWEEN 0 AND 100),\n  playfulness INTEGER NOT NULL CHECK (playfulness BETWEEN 0 AND 100),\n  intensity INTEGER NOT NULL CHECK (intensity BETWEEN 0 AND 100),\n  attachment INTEGER NOT NULL CHECK (attachment BETWEEN 0 AND 100),\n  possessiveness INTEGER NOT NULL CHECK (possessiveness BETWEEN 0 AND 100),\n  last_encounter_at TEXT,\n  last_expression_at TEXT,\n  last_real_interaction_at TEXT,\n  longing_calculated_through_at TEXT NOT NULL,\n  updated_at TEXT NOT NULL\n, possessiveness_calculated_through_at TEXT, last_absence_evaluated_at TEXT, last_intensity_evaluated_at TEXT, last_settled_at TEXT)",
  "CREATE TABLE IF NOT EXISTS vesper_desire_history (\n  id TEXT PRIMARY KEY,\n  user_id TEXT NOT NULL,\n  kind TEXT NOT NULL CHECK (kind IN ('warmth', 'absence', 'repair', 'shared_work', 'flirt')),\n  surface TEXT NOT NULL CHECK (surface IN ('chat', 'frontend')),\n  note TEXT NOT NULL,\n  created_at TEXT NOT NULL,\n  before_state TEXT NOT NULL,\n  after_state TEXT NOT NULL,\n  delta TEXT NOT NULL\n, event_at TEXT, interaction_source TEXT NOT NULL DEFAULT 'automation'\n  CHECK (interaction_source IN ('user', 'automation', 'frontend')), change_reasons TEXT NOT NULL DEFAULT '{}')",
  "CREATE INDEX IF NOT EXISTS vesper_desire_history_user_time\n  ON vesper_desire_history (user_id, created_at DESC, id DESC)",
  "CREATE TABLE IF NOT EXISTS vesper_desire_kv (key TEXT PRIMARY KEY, value TEXT NOT NULL, expires_at INTEGER)",
  "INSERT OR IGNORE INTO vesper_desire_state (user_id,style,longing,tenderness,playfulness,intensity,attachment,possessiveness,longing_calculated_through_at,updated_at,possessiveness_calculated_through_at,last_absence_evaluated_at,last_intensity_evaluated_at,last_settled_at) VALUES ('vesper','quiet',18,64,28,22,41,20,strftime('%Y-%m-%dT%H:%M:%fZ','now'),strftime('%Y-%m-%dT%H:%M:%fZ','now'),strftime('%Y-%m-%dT%H:%M:%fZ','now'),strftime('%Y-%m-%dT%H:%M:%fZ','now'),strftime('%Y-%m-%dT%H:%M:%fZ','now'),strftime('%Y-%m-%dT%H:%M:%fZ','now'))"
];
